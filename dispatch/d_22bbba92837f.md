# Dispatch brief: ddesbons/velocity-systems-site

**dispatch_id:** `d_22bbba92837f`
**target_repo:** `ddesbons/velocity-systems-site`
**priority:** p1
**commitment_source:** `gmail:19d9e294f4e4bd33`
**entity:** `ent_b1ccc6a7ed11a933e56edc01`

## Original commitment

> Bug de config cote header. Je corrige avant lundi. (Velocity Systems site — header/legal-page nav misconfiguration flagged by David Desbons in WhatsApp + email thread on 2026-04-17 21:16. Bounded fix scoped to navigation config in the public site.)

## Classifier verdict

Decision: **dispatch**
Scores: repo_bounded=0.95, judgment_free=0.90, reversible=0.98
Rationale: Single-file nav/header config fix in an allow-listed repo. No client-voice content, no architectural choice, no destructive op. Reversible by design (PR-gated, no DB/secret writes). Anchored to a concrete deliverable Simon already promised David.

## Deliverables

A draft PR against the target repo that addresses the commitment quoted above. Stay within the bounded scope; do not refactor outside what the commitment implies.

## Acceptance criteria

- PR is a **draft** (never ready-for-review without Simon's explicit request).
- Change is scoped to what the commitment describes; no incidental refactor.
- All existing tests still pass.
- PR body references this brief's `dispatch_id` (`d_22bbba92837f`).
- No production DB migrations, secret rotations, or destructive ops.

## Files to touch (best guess)

Cloud CC to determine based on repo layout. Stay within the module or component implied by the commitment. If the bounded change turns out to cross module boundaries, stop and surface a question in the PR description rather than expanding scope.

## Allow-list reference

`ddesbons/velocity-systems-site` must be present in `lemonclaw/dispatch/config/dispatch-allowed-repos.yaml` at dispatch time. The dispatcher enforces this before creating the issue (exit 3 otherwise).

## Cost cap

$25/mo combined Anthropic + OpenAI. Tracked in `lemonclaw/dispatch/lib/cost.py` against `state/api-costs.jsonl`; `CapExceededError` fires at $24 MTD with a 50¢ buffer.

## No auto-merge

Cloud CC opens a **draft PR**. It does **not** merge. Simon reviews and merges manually every time. Any auto-merge attempt is a contract violation: abort and leave the PR as draft.

## Neotoma source observation

This dispatch traces back to commitment `gmail:19d9e294f4e4bd33` on entity `ent_b1ccc6a7ed11a933e56edc01`. Lifecycle transitions are written as observations on the dispatch task in Neotoma (`localhost:13180 --api-only`). Do not mutate entity fields directly.
